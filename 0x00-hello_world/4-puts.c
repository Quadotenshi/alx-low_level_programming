#include <stdio.h>
/**
 * main - using the puts function
 * Return: value 0
 */
int main(void)
{
	char myline[] = "\"Programming is like building a multilingual puzzle";

	puts(myline);
	return (0);
}
