#include <stdio.h>
/**
 * main - function to print alphabet
 * Return: to print cap and small
 */

int main(void)
{
	int x;

	for (x = 'a'; x <= 'z'; x++)
		putchar(x);
	for (x = 'A'; x <= 'Z'; x++)
		putchar(x);
	putchar('\n');
	return (0);
}
